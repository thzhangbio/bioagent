# MVP 实现路线图

> 版本：v0.4
> 日期：2026-04-04
> 前置文档：产品蓝图.md（§〇 极简 RVP 边界 + 「知识库与 RAG」核心原则）  
> **知识库分层、文献库、`wechat_style` 与远期溯源规划**：见 **知识库分层与文献库规划.md**；**目录与脚本清单**：见 **项目结构报告.md**

---

## 项目结构（目标形态，含里程碑 2 起增量）

```
bioagent/
├── docs/
│   ├── 产品蓝图.md
│   ├── 实现路线图.md
│   ├── 知识库分层与文献库规划.md
│   └── 项目结构报告.md
├── research/                    # 市场调研（归档，不参与 tsc 默认编译）
├── scripts/                     # tsx 直跑：ingest:*、search:*、probe:*
├── side-tools/
│   └── pdf-text-cleanup/        # MinerU → *.kb.md 侧车工具；pnpm run pdf-kb-pipeline
├── src/
│   ├── index.ts
│   ├── lark/                    # 飞书：事件、消息、文档、交互卡片
│   ├── knowledge/               # RAG：分集合向量库（里程碑 2；多租户后移）
│   │   ├── ingest.ts            # embedTextChunks、buildMergedStore
│   │   ├── retrieve.ts          # retrieve(query, { collections?, wechatStyleVariants?, topK })
│   │   ├── collections.ts       # COLLECTION_* 常量
│   │   ├── types.ts             # literature | wechat_style | …；WechatStyleVariant
│   │   └── doi.ts               # 文献 DOI 解析（ingest:literature）
│   ├── agent/
│   │   ├── router.ts
│   │   └── workflows/           # content-create 等，生成前须调用 retrieve
│   ├── memory/
│   │   └── store.ts             # 本地 JSON（M1）；结构化记忆迁移见 M3
│   └── prompts/
├── data/                        # 运行时（gitignore）：memory、rag-store.json、uploads、knowledge/literature-inbox/ …
├── .env
├── package.json
├── tsconfig.json
└── .gitignore
```

**说明**：**合规规则扫描**（规划中的 `compliance.ts` 类模块）里程碑 2 仍可按任务 2.5 落地；当前仓库以 **RAG `medical` 预置片段 + 提示词约束** 为主，**非**必须已存在 `src/medical/compliance.ts` 文件才验收里程碑 2。

---

## 技术选型

| 维度 | 选择 | 理由 |
|------|------|------|
| 语言 | TypeScript | 类型安全，与飞书 SDK、前后端工具链一致 |
| 运行时 | Node.js | 18+ 推荐；飞书 SDK 与异步 IO 适合长连接与批处理 |
| 飞书交互 | `@larksuiteoapi/node-sdk` | 长连接事件、发消息、文档等统一身份；CLI 仅作本地调试辅助 |
| LLM | Claude API（可经中转） | 理解与生成；**事实与调性以 RAG 为准** |
| **向量与 RAG** | **Embedding API + 本机 JSON 向量库（`data/knowledge/rag-store.json`），可演进 Postgres+pgvector** | **多集合可检索**（`platform_tone` / `medical` / `personal` / **`literature`** / **`wechat_style`** 等）、个人上传可走通；**不追求**分布式与极限 QPS；选型以 **少依赖、易 Git 回退** 为先 |
| 包管理 | pnpm | 快速、节省磁盘 |

---

## 里程碑 1：能说话

### 前置准备

**步骤 1：飞书开放平台配置**
- 登录 https://open.feishu.cn，创建自建应用
- 获取 App ID 和 App Secret
- 开启机器人能力
- 配置权限：im:message（收发消息）、im:chat（群聊管理）
- 配置事件订阅：im.message.receive_v1

**步骤 2：本地环境搭建**
- 安装飞书 CLI：`npm install -g @larksuite/cli`
- 安装 AI Skills：`npx skills add larksuite/cli -y -g`
- 配置认证：`lark-cli config init`（输入 App ID / App Secret）
- 登录：`lark-cli auth login --recommend`
- 验证：`lark-cli doctor`

**步骤 3：项目初始化**
- `pnpm init`
- 安装依赖：typescript、`@larksuiteoapi/node-sdk`、`@anthropic-ai/sdk` 等
- 配置 tsconfig.json
- 配置 .env（`FEISHU_APP_ID`、`FEISHU_APP_SECRET`、`ANTHROPIC_API_KEY` 等，见 `.env.example`）

### 开发任务

**任务 1.1：消息接收**
- 用 `@larksuiteoapi/node-sdk` 的 `WSClient` 长连接监听 `im.message.receive_v1`
- 解析消息体：提取发送人、消息内容、对话 ID
- 目标：在终端能看到用户发来的每条消息（与当前仓库 `src/lark/events.ts` 一致）

**任务 1.2：意图识别**
- 编写 System Prompt（岗位说明书）：定义 AI 的角色、能力范围、回复风格
- 将用户消息 + System Prompt 发给 Claude API
- Claude 返回：意图分类 + 回复内容
- 五类意图：布置任务 / 提供信息 / 修改反馈 / 询问建议 / 岗位外请求

**任务 1.3：消息回复**
- 将回复通过飞书 API（`im.v1.message` create/reply）发回用户
- 先实现纯文本回复，跑通闭环
- 然后升级为飞书交互卡片（Interactive Card）回复

**任务 1.4：入职对话**
- 首次对话时，AI 主动提问了解公司信息
- 对话式收集：公司名称、产品、目标客户、品牌调性、竞品
- 将收集到的信息存入本地 JSON 文件（里程碑 1 简化方案，里程碑 3 迁移到飞书 Base）

### 验证标准

5 句测试：
1. "帮我写一篇关于我们新产品的小红书笔记" → 识别为内容创作任务
2. "最近写什么话题好" → 识别为选题建议
3. "我们是做医美的，主打玻尿酸填充" → 识别为公司信息，存入记忆
4. "上次那篇改一下，开头太平了" → 识别为修改反馈
5. "'3天见效的抗衰秘密'这个标题行吗" → 识别为审核，指出广告法风险

---

## 里程碑 2：能干活 + 有据可依（RAG v1）

> **原则对齐（产品蓝图 §〇）**：本里程碑交付 **单用户/本机可用的 RAG 最小闭环**——**平台调性库 + 医学/专业库 + 个人上传与偏好库 + 文献事实库（`literature`，已工程落地）** 均可检索注入；**微信风格库（`wechat_style`）为可选**，用于表达层参考；**刻意不做**多租户工程与极限负载。**必须落实**客户资料的 **最低成本安全与隐私**（密钥、落盘路径、日志最小化，见任务 2.0）。飞书文档交付、多平台、基础规则扫描、改稿与 RAG **同一阶段完成**。

### 前置条件
- 里程碑 1 全部完成
- 飞书应用增加权限：**docx:document**（云文档创建/编辑）；后续按需增加 **drive**（若从云空间拉文件）
- 可选：卡片相关 **cardkit** 权限（交付通知用交互卡片）

### 开发任务

**任务 2.0：RAG 基础设施（必须先于或与 2.1 并行落地）**
- **向量库（本机/单部署）**：当前实现为 **统一 `rag-store.json` 多集合**；元数据带 `collection`（如 `platform_tone` | `medical` | `personal` | **`literature`** | **`wechat_style`**），**`wechat_style`** 块可带 **`wechatStyleVariant`（`liangyi_hui` / `medsci`）** 供子风格过滤；**不实现多租户隔离**（后续再演进到 `tenant_id`）。
- **入库管道**：文本分块 → Embedding → 写入/合并；**个人资料**：私聊上传附件 → `personal` 追加；**预置**：`ingest:presets` / `ingest:jobs`；**文献**：`data/knowledge/literature-inbox/*.md` → **`pnpm run ingest:literature`**（仅替换 `literature` 集合）；**期刊清洗稿**：`side-tools/pdf-text-cleanup` 产出 `*.kb.md` 再拷贝至 inbox；**微信风格语料**：规划独立灌库（或手工写入带 `wechatStyleVariant` 的块），**默认不阻塞**里程碑 2 闭环。
- **检索接口**：`retrieve(query, { collections?, wechatStyleVariants?, topK })` → 拼接进 content-create / 对话 RAG Prompt；日志**避免打印客户原文**，仅保留可追溯的短摘要或 ID。
- **预置库**：调性/运营参考、医学与合规片段（可先小后大，但必须可检索）；**文献库**与 **微信风格库**分层及调用策略见《知识库分层与文献库规划.md》。
- **隐私底线（最低成本）**：`.env` / 密钥不入 Git；`data/` 下向量与上传文件 **gitignore**；文档说明 **哪些数据会发往 Embedding/LLM**；可选：本机目录权限提示、敏感字段不落日志。

**任务 2.0 — 隐私底线 · 验收勾选（里程碑 2 必过）**

| # | 验收项 | 说明 |
|---|--------|------|
| ☐ | **密钥与凭证** | `.env`、飞书/模型密钥仅本机或部署环境注入；仓库内仅有 `.env.example`，无真实值；文档或 PR 说明不提交密钥。 |
| ☐ | **运行时数据不入版本库** | `data/`（含向量索引、上传缓存、`memory.json` 等）在 `.gitignore` 中；新产生的客户文件路径不硬编码进公开仓库。 |
| ☐ | **外发数据可知** | `README` 或 `docs` 中有一段说明：**哪些内容**会发往 Embedding API 与 LLM（例如：分块正文、元数据字段列表），避免无意识全量粘贴。 |
| ☐ | **日志最小化** | 默认日志不打印客户上传**全文**与对话**全文**；必要时仅 ID、哈希、前 N 字摘要；调试开关单独控制且默认关闭。 |
| ☐ | **本机边界** | 极简 MVP 默认仅本机/可信环境运行；若文档中给出「勿在共享电脑裸跑」等提示，视为满足（可选但推荐）。 |

**任务 2.1：内容创作工作流**
- 用户说"写一篇XX" → router 分发到 `content-create` workflow
- Workflow 步骤：
  1. 从记忆系统读取公司画像
  2. **RAG 检索**（用户库 + 预置库，按平台与主题构造 query）
  3. 根据目标平台确定内容风格模板，与检索结果一并注入
  4. 调用 Claude 生成（须约束：优先依据检索片段，避免无引用编造）
  5. 合规检查（`compliance.ts` + 可选合规类检索片段）
  6. 使用 **飞书 Node SDK** 创建云文档并写入正文
  7. 发送通知（文本链或交互卡片，含文档链接；可注明「已参考知识库」类摘要）

**任务 2.2：多平台适配**
- 同一话题，不同平台输出完全不同的内容
- 平台风格模板：小红书 / 公众号 / 知乎；**检索 query 随平台变化**（不同库权重可配置）
- 模板作为 prompt 的一部分注入

**任务 2.3：改稿（聊天路径）**
- 用户在聊天中说"改一下XX" → router 识别为修改意图
- Agent 定位最近一次交付的文档；**改稿前对修改意图 + 段落主题做 RAG 检索**（用户禁忌词、术语表等）
- 调用 Claude 修改；使用 SDK 更新云文档
- 回复对比说明或卡片

**任务 2.4：改稿（文档批注路径）**
- 订阅/监听文档评论相关能力（以开放平台当前事件为准）
- 读取批注内容与位置 → **可选检索** → 修改对应段落 → 回复批注

**任务 2.5：合规检查模块**
- 内置规则库（广告法绝对化、医疗违规表述、化妆品宣称等）
- 每篇交付前扫描；可与 **合规类向量片段** 联合输出「依据」
- 自动修正时说明理由

### 验证标准

1. "写一篇关于玻尿酸新品的小红书笔记" → 飞书文档交付；风格、医学表述、转化与合规达标；且 **检索链路可观测**（日志或交付说明）
2. "这个话题再写一篇公众号长文" → 风格明显不同，非简单改写
3. 在文档中批注"这段改口语化" → 精确改段落
4. **用户上传资料** → 进入专属库后，后续生成 **能利用** 其中内容（可通过针对性命题验收）
5. **隐私底线**：上表「任务 2.0 — 隐私底线 · 验收勾选」**五项全部勾选**（或等价实现并有简短记录）

---

## 里程碑 3：能成长

### 前置条件
- 里程碑 2 全部完成（含 **RAG v1 与多集合 + 隐私底线**）
- 飞书应用增加权限：bitable:table（多维表格）、wiki:node（知识库）、calendar:event（日历）、task:task（任务）

### 开发任务

**任务 3.1：记忆与协作载体（飞书 Base / Wiki）+ 与 RAG 协同**
- 将里程碑 1 起的本地结构化记忆 **迁移或同步** 至飞书多维表格（公司画像、交互记录、优秀案例等表结构同原规划）
- **Wiki / 云文档**：作为品牌与案例的**人工可编辑沉淀**；**可选同步管道**：Wiki/指定文件夹变更 → 触发 **重新分块入库**，保持向量库与飞书内容一致（策略：全量/增量由实现阶段定）
- 生成前：除 **向量检索** 外，**按需读取 Base 行级字段**（如强约束字段），与 RAG 结果合并为上下文
- **不替代** 里程碑 2 已交付的向量库；本任务侧重 **运营、协作与审计**，与 RAG **互补**

**任务 3.2：主动建议**
- 定时任务（如每周一早上 9:00）
- Agent 检索行业热点（通过联网搜索或预设信息源）
- 结合公司画像判断相关性
- 如果有值得跟进的话题，主动发送建议卡片
- 频率控制：每周不超过 2 次

**任务 3.3：内容排期**
- 用户说"帮我排一下这个月的计划" → Agent 生成排期方案
- 用 **OpenAPI/SDK** 写入多维表格（日期、选题、平台、状态）
- 同步飞书日历与任务（同上）
- 到期自动提醒 + 开始写初稿（可结合 RAG 选题建议）

**任务 3.4：周报/复盘**
- 每周五自动汇总本周完成的内容
- 输出：完成了几篇、哪些已发布、用户反馈统计
- 建议：下周重点方向
- 以飞书卡片消息发送

### 验证标准

1. 第 1 天说的公司信息，第 30 天 AI 自动使用，不用重复（含 Base 与/或向量侧记忆）
2. AI 主动发的话题建议，用户觉得有道理（可结合 RAG 与热点源）
3. AI 生成的周计划，用户基本只需微调

---

## 实施顺序总结

```
第 1 步：飞书开放平台创建应用 + 本地环境搭建
第 2 步：项目初始化（TypeScript + 依赖）
第 3 步：任务 1.1 — 能收到消息
第 4 步：任务 1.2 — 能理解意图
第 5 步：任务 1.3 — 能回复消息（先文本，再卡片）
第 6 步：任务 1.4 — 入职对话 + 基础记忆
  → 里程碑 1 验证 ✓

第 7 步：任务 2.0 — RAG 基础设施（向量库、入库、检索、多集合含 literature、上传入口、隐私底线）
第 8 步：任务 2.5 — 合规检查模块（可与 2.0 并行）
第 9 步：任务 2.1 — 内容创作工作流（生成前必须 retrieve）
第 10 步：任务 2.2 — 多平台适配
第 11 步：任务 2.3 — 聊天改稿
第 12 步：任务 2.4 — 文档批注改稿
  → 里程碑 2 验证 ✓

第 13 步：任务 3.1 — Base/Wiki 与 RAG 协同、记忆运营化
第 14 步：任务 3.2 — 主动建议
第 15 步：任务 3.3 — 内容排期
第 16 步：任务 3.4 — 周报/复盘
  → 里程碑 3 验证 ✓
```

**说明**：里程碑 2 中 **2.0 与 2.1 强耦合**——验收「能干活」时须同时验收「有据可依」；顺序上可先搭好最小向量链路与空库检索，再接入真实用户上传与预置库加厚。
