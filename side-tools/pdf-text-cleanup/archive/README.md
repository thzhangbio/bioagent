# 归档（已跑完 pipeline 的 MinerU 原始稿）

`inbox/` 中**已**用 `pdf-kb-pipeline` 生成 `out/` 下对应 `*.kb.md` 后，将**原始** MinerU 导出移入本目录，避免与待处理稿件混淆。

建议一并归档：

- 同名的 **`.md`**（MinerU 正文）
- 若曾使用，同名的 **`.json`**（MinerU 版面结构，体积可能较大）

**不移动** `out/` 中的终稿 `.kb.md`（产出保留在 `out/` 或再复制到 `data/knowledge/literature-inbox/` 做向量入库）。
