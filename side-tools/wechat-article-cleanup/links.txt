# 微信公众号文章链接：每行一条 URL。以 # 开头的行为注释。
# 去重：同一 URL 只处理一次。
# 流水线：pnpm run wechat-article-pipeline（抓取 → inbox → 清洗 → out）
# 若抓取为验证页，可将浏览器另存的完整 HTML 放入 inbox/，命名 *.raw.html，再：
#   pnpm run wechat-article-pipeline -- --clean-only
#
# 归档 ①（out 满意后）：pnpm run wechat-article-archive-inbox  →  archive/processed-inbox/
# 归档 ②（向量化入库后）：pnpm run wechat-article-archive-out  →  archive/ingested-out/
